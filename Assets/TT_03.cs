using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TT_03 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("tt03");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
